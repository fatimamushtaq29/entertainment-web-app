import Layout from '../components/Layout';
import SectionContainer from '../components/SectionContainer';

export default function TVSeriesPage() {
  return (
    <Layout>
      <SectionContainer headingText="TV Series" mediaType="series" />
    </Layout>
  );
}
